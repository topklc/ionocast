# ionocast Methodology Paper — Independent Audit (2026-05-07)

Audit target: `paper/ionocast-methodology.tex` (6209 lines, 72-page PDF)
against the codebase under `src/`, `scripts/harness.mjs`, and
`functions/`. Repository at the audit commit. Performed without
reading the prior `METHODOLOGY-AUDIT-2026-05-06.md` or its triage.

The repository is treated as the source of truth: where the paper and
the code disagree, the paper is flagged as documentation drift.

## Findings table

| ID | Severity | Finding | Paper line(s) | Evidence | Suggested fix |
|----|----------|---------|---------------|----------|---------------|
| 1 | Critical | Paper Eq.~30 (`eq:fof2-nightdecay`) prose says the night-decay multiplier `max(0.6, 1+0.4 cosχ)` operates "on the already-constructed `b`", i.e. on `b_floor + 4.0·P(φ)·d`. Code applies it only to `base` (the `b_floor` term) BEFORE adding `dayBump * driver`. The two formulations diverge through the early-evening tail when `driver > 0` and `cosχ < 0`. | 2120-2129 | `src/physics/climatology.js:101-103` multiplies only `base`, then adds `dayBump * driver` after, producing `base*decay + dayBump·driver` rather than `(base + dayBump·driver) * decay` | Either change paper Eq.~30 to state the multiplier acts on `b_floor` only (matching code), or change code to multiply the constructed `foF2` after `dayBump * driver` is added |
| 2 | Critical | Paper Table 6 (`tab:graylineamps`, line 3181) lists 20 m grayline bonus = 1.5 dB sunrise / 1.0 dB sunset. Code zeroes the bonus for any band above 14 MHz exactly: `if (fMHz > 14) return 0`. 20 m is 14.097 MHz → 14.097 > 14 → bonus = 0. The 20 m row in the table is unreachable in production. | 3181 | `src/physics/modes.js:284`; `BAND_FREQ_MHZ["20 m"] = 14.097` in `src/constants.js:44` | Either lift the gate to `fMHz > 16` (admitting 20 m), or remove the 20 m row from Table 6 and amend the prose at 3189 to read "above 14 MHz including 20 m" |
| 3 | Critical | Paper claims hp30 (`/api/hp30`) is a geo-resolved feed taking a Maidenhead grid query parameter. Code: `functions/_handlers/hp30.js` fetches a single global text file and contains no Maidenhead parsing. Only `/api/giro` and `/api/tropo` perform grid → station resolution. | 269-272 | `functions/_handlers/hp30.js`; only `giro.js:42`, `tropo.js:22` define `maidenheadToLatLon` | Drop `/api/hp30` from the geo-resolved feed list; the abstract's "two panels that need a nearest-station lookup" (line 86) is the correct count |
| 4 | Critical | Paper Table 11 (`tab:soft-alerts`) lists Dst severity thresholds at -150 nT (alert "DST SEVERE"), -100 nT (alert "DST STRONG"), -50 nT (watch "DST MODERATE"). Code has thresholds at -250 nT (extreme), -100 nT (alert), -50 nT (watch). The -150 nT level in the paper does not exist in the code, and the -250 nT extreme tier is not in the paper. | 5242-5244 | `src/ui/builders/alerts.js:236-238` (`_dstRule`) | Replace paper Table 11 Dst rows with -250 (extreme), -100 (alert), -50 (watch) |
| 5 | Critical | Paper Table 11 lists the storm-Kp severity ladder as Kp≥9, 8, 7, 6 → alert and Kp≥5 → watch. Code emits "extreme" (not "alert") for Kp≥9 (G5) and Kp≥8 (G4); the four-level ladder paper documents misses the "extreme" tier entirely. The whole alerts pipeline runs on five severity levels (info, watch, alert, **extreme**) but the paper consistently states four levels (info, watch, warn, alert; lines 5197-5198, table caption 5224-5225). | 5197-5208, 5224-5237 | `src/ui/builders/alerts.js:214-218`, intra-tier sort prose at 5197 | Add "extreme" as the top severity level wherever the paper enumerates {info, watch, warn, alert}; update Kp G4/G5 rows to "extreme"; update Dst, X-ray, proton-flux, and aurora rows accordingly |
| 6 | Critical | Paper Table 11 lists soft alert proton-flux gates at S1 (≥10 pfu, watch), S2 (≥100, alert), S3 (≥1000, alert). Code additionally emits S4 (≥10 000 pfu, extreme) and S5 (≥100 000 pfu, extreme). The paper-cited "Eight rules" (line 5187) and Table 11 truncate the upper tail of the actual NOAA S-scale that the code implements. | 5252-5254 | `src/ui/builders/alerts.js:262-271` | Add S4 and S5 rows with the "extreme" severity to Table 11 |
| 7 | Critical | Paper Table 11 lists X-ray flare rules at "≥M5 alert" and "M1–M4.9 watch". Code subdivides into R1 (M1–M4.9 watch), R2 (M5–M9.9 alert), R3 (X1–X9.9 alert), R4 (X10–X19.9 extreme), R5 (≥X20 extreme). The paper compresses the X-class subdivision out and again drops the extreme tier. | 5230-5231 | `src/ui/builders/alerts.js:189-208` (`_flareRule`) | Replace the two paper rows with the five-row R1–R5 ladder the code implements |
| 8 | Critical | Paper Table 11 lists aurora HP at "HP≥100 watch" and "HP≥50 info". Code emits "HP≥100 alert" and "HP≥50 watch" — a one-tier-higher escalation across the board. | 5246-5247 | `src/ui/builders/alerts.js:241-247` (`_auroraRule`) | Replace aurora row severities with alert / watch |
| 9 | Critical | Paper claims the soft-alert pipeline is "Eight rules" (line 5187) and Table 11 enumerates 8. Code runs ten rules: the eight in the paper plus `_hssRule` (high-speed-stream front; SW speed ≥ 600 km/s) and `_forecastSigmaRule` (forecast-σ inflation ≥ 2 dB). | 5187, 5220-5258 | `src/ui/builders/alerts.js:289-310` | Update prose to "Ten rules" and add HSS and forecast-σ rows to Table 11 |
| 10 | Critical | Paper §7.3.1 Eq. (line 4304-4307) defines the forecast σ catch-up ramp as `min(1, max(0, K_p^peak - K_p^eff))` and the surrounding paragraph (4308-4313) explicitly says "gated on `K_p^eff` rather than the live 3-hour `K_p`". Code uses the **live** `K_p` (`kpNow`), not `K_p^eff`. | 4304-4313 | `src/derive/conditions.js:136` passes `kpNow` to `forecastKpPenaltyDb`; `src/derive/storm.js:128-132` computes `gap = peakIn6h - currentKp` | Either change the code to pass `kpEffective`, or change the paper to read "live `K_p`". As written, both the equation and the supporting paragraph are inconsistent with code |
| 11 | High | Paper §7.3 enumerates "five [σ penalties] below plus the Es-active mode contribution" = six penalties, and the explicit RSS formula at line 4353-4355 sums six terms (`σ_g², σ_MUF², σ_storm², σ_forecast², σ_term², σ_recovery², σ_Es²`). Code adds a seventh: a +3 dB (variance 9 dB²) night-time inflation when `cosZpath < 0` and `fMHz ≤ 16`. | 4250-4359 | `src/physics/snr.js:391-393` | Add a "Night-time low/mid HF inflation" entry to the bulleted list and to the RSS formula at 4353; document the `f ≤ 16 MHz`, `cosZpath < 0` gate |
| 12 | High | Paper Table 11 caption (line 5222-5224) maps severity to "info / warn / watch / alert; alert ranks highest". Body prose (5198) repeats the four-level claim. The intra-tier sort order paragraph (5198: "alert > watch > warn > info") never mentions "extreme", which the code uses as the top tier. | 5198, 5222-5225 | `src/ui/builders/alerts.js:194` and elsewhere — `level: "extreme"` literal | Insert "extreme" as the top tier in the sort order documentation and the table caption |
| 13 | High | Paper line 5213-5215 contains a literal source-line reference: "per~\S\ref{sec:fast-storm-drivers} line~2811". Line 2811 does not contain the cited Dst-response prose (which now lives at line 2980). Worse, the rendered PDF has no source-line numbers, so "line 2811" is meaningless to a reader. | 5213-5215 | `paper/ionocast-methodology.tex:5214` and the actual Dst prose at 2980 | Remove "line~2811"; the section reference alone is sufficient |
| 14 | High | Paper §3.5.1 prose (lines 1066-1070) refers to "the $\phi_{\text{CGM}}$ ramp's $5\text{\textdegree}$ smoothing width in Eq.~\ref{eq:laur}". The smoothing-width definition lives in the equation block at 991-998 (`c(φ)` ramp), not in `eq:laur` (which is line 925). The cited equation does not contain the 5° width. | 1066, 1081 | `paper/ionocast-methodology.tex:925-997` | Reference the `c(φ)` definition block (or assign a label like `eq:cphi`) rather than `eq:laur` |
| 15 | High | Paper line 5896-5916 closes with "Every constant in the SNR budget … is given inline" but several constants the budget actually depends on are not stated anywhere in the paper. Examples: per-hop and path caps for the auroral term (`PCA_PER_HOP_CAP_DB = 30` and `PATH_AUR_CAP_DB = 50` are stated; the matching auroral per-hop cap of 30 dB is hard-coded in `lAuroralDb` rather than via a named constant), and `L_IONO_AUR_DB = 25` (the VHF auroral lumped loss used by `snrMarginVhfAurora`). | 5897-5904, 3463 | `src/physics/loss.js:299, 335` and `src/physics/loss.js:66` | Either name the auroral per-hop cap constant and document it; explicitly tabulate `L_IONO_AUR_DB = 25 dB` near §3.13 / §6.4 |
| 16 | High | Paper §6.5 line 3784 claims the TEP plateau "$\sim\!8.5$\,dB at quiet sun ($F_{10.7\text{A}} = 70$)". Direct evaluation of `tepBonusMaxDb(70) = 8 + 7·sigmoid(-55/30)` gives 8.97 dB, not 8.5 dB. The other quoted points (11.2 at 120, 14.0 at 180) match the formula. | 3784 | `src/physics/modes.js:44-48` | Update the paper to "$\sim\!9$ dB at quiet sun" |
| 17 | High | Paper line 5364-5365 claims EIA crest "+50% of the base foF2 at $F_{10.7\text{A}} = 70$, saturating to +85% at $F_{10.7\text{A}} \ge 120$". This is correct mathematically. But the parallel trough claim immediately after — "saturating to $-50\%$ at $F_{10.7\text{A}} \ge 170$" — uses the asymptotic-saturation epoch only at the lower bound; the paper should match style. The crest's body prose (lines 2229-2231) and limits prose (5364-5367) use different conventions for citing the saturation epoch (≥120 vs. ≥170). | 5364-5367 | `src/constants.js:127-128, 154-156` (slope 0.007 → cap at F=120; slope 0.002 → cap at F=170) | Make the body and limits paragraphs cite the saturation epoch consistently |
| 18 | High | Paper line 4255-4270 footnote enumerates the overloaded uses of `σ`. The list includes $\sigma_{Es}$ as a prediction-spread σ, and the paper's RSS formula at 4353 includes `σ_Es²`. But the `σ_Es` term is not added inside `snrMarginHf` (`src/physics/snr.js`); the `σ_Es² = (2 dB)²` quadrature contribution must be added by the *caller* when an Es-mode wins the bonus competition, and `derive/conditions.js` does not surface a code path that explicitly RSS's it into the F2 sigma. The paper's RSS as written is therefore not the formula `snrMarginHf` evaluates. | 4353-4359 | `src/physics/snr.js:342-405` (no σ_Es term); paper claim at 3327-3328 | Either document where in the call chain `σ_Es` is folded in, or remove `σ_Es²` from the RSS at 4353 |
| 19 | High | Paper §3.3 line 583 defines the D-RAP absorption equation `eq:labs` using `r = HAF/f`, with branches for `r < 0.25`, `0.25 ≤ r < 0.30` and `r ≥ 0.30`. The continuous-ramp form is correct. But the section heading and subsequent text refer to "flare-enhanced" absorption while `eq:labs` is the D-RAP HAF model and the per-hop flare-driven term is `eq:lflare` (§3.3c). The two terms are then said to be combined via `max(L_DRAP, ΣL_flare)` (line 602). The paper's variable name `L_DRAP` (used in eq:flareabs-inline at line 446) is not the same as `L_abs` used in `eq:labs` (line 577) or as the function name `lAbsDb` in code. The naming is inconsistent across §3.3 / §3.3c / Eq.~\ref{eq:flareabs-inline}. | 446, 577, 602 | `src/physics/loss.js:89` is `lAbsDb`; `src/physics/snr.js:285` collapses via `Math.max(lAbs, lFlare)` | Rename consistently to `L_DRAP` in §3.3, or to `L_abs` in §3.3 / §3.3c |
| 20 | High | Paper line 5559 lists "Spread $\sigma_g$ (base) & 8--12\,dB per band" but the actual `BAND_SIGMA_DB` table values for low-mid bands sit at 8 dB (160m–40m), 9 dB (30m, 20m, 17m), 10 dB (15m), 12 dB (12m, 10m). The historical comment in `src/constants.js:218-231` (and earlier paper drafts) cited "6 dB" as the floor; some paragraphs of the paper still reference the 6 dB ITU-R P.533 floor (line 1394 "circuit-reliability framework around the per-band $\sigma$" ≥ 6 dB; line 4058 "ITU-R P.533 day-to-day spread range 6--12 dB"). This makes the paper's quoted range alternate between "6–12" (P.533 anchor) and "8–12" (actual ionocast pick), which is technically defensible but confusing. | 4058, 5559, 5564 | `src/constants.js:218-231`; `src/physics/tier.js:12` (`DEFAULT_SIGMA_DB = 8`) | Pick a single quoted range and note explicitly that ionocast's actual minimum is 8 dB (above the P.533 6 dB floor) wherever both ranges are mentioned |
| 21 | High | Paper line 1576-1577 caption of Table 4 says "Re-derived 2026-04-30 from P.372-15 Fig 13 (atmospheric) ⊕ Fig 23 (galactic) max-of at midlat midnight summer." The bibliography (line 5931-5935) cites P.372-14 (2019), not P.372-15. The code comment in `src/constants.js:53` repeats "P.372-15" as the source of the re-derivation. | 1581, 5931-5935 | `paper/ionocast-methodology.tex:5932` cites P.372-14 | Update the table caption (and the matching code comment) to cite the version actually used, or update the bibitem to P.372-15 if that exists |
| 22 | High | Paper line 5187 says rules run "against the same data bundle the per-band budget consumes ($K_p$, $B_z$, $D_{\text{st}}$, aurora HP, DRAP, GOES X-ray, GOES ≥10 MeV protons, and storm-lag effective $K_p$)." That is 8 input fields. Code consumes additionally `swSpeed` (HSS rule) and `forecastSigmaDb` (forecast-σ rule). | 5187 | `src/ui/builders/alerts.js:289-310` | Add `solar-wind speed` and `forecast σ` to the listed inputs |
| 23 | High | Paper §3.2 footnote at line 692-699 describes the K9LA D-region table as "the per-band entries reflect amateur quiet-day observations and are anchored to those, with P.533 used as a sanity-check overlay rather than the source." But code-side comment (`src/physics/loss.js:138-156`) explicitly states "Base values calibrated 2026-04-30 against P.533 §A.2 quiet-day non-deviative absorption formula" — the values are now P.533-derived (`L_P.533(f)/1.6`), not K9LA-derived. The paper's claim that K9LA is the anchor is stale. | 685-699 | `src/physics/loss.js:138-156` documents the P.533 derivation chain | Update the paper's source attribution from "K9LA quiet-day amateur-band estimates" to "P.533 §A.2 non-deviative absorption divided by 1.6 for oblique-incidence correction, anchored to the K9LA range as a sanity overlay" |
| 24 | High | Paper line 4353-4354 RSS formula sums `σ_g² + σ_MUF² + σ_storm² + σ_forecast² + σ_term² + σ_recovery² + σ_Es²`. Code's actual RSS in `snrMarginHf` adds (1) `sigBase²`, (2) `mufPenalty²` for f/MUF > 0.85, (3) `stormPenalty²` for kp ≥ 5, (4) `forecastSigmaDb²`, (5) `9` (variance) for cross-terminator, (6) `9` for night-time low/mid HF, (7) `16` for storm-recovery. That is seven contributions — the paper enumerates only five plus Es. | 4271-4359 | `src/physics/snr.js:342-404` | Rewrite the RSS formula and the bullet list to enumerate all seven code-implemented penalties (the missing one is the night-time low/mid σ inflation) |
| 25 | High | Paper line 4280-4283 says `σ_storm = 3 + 0.75 (K_p^eff − 5) dB`, "+3 dB at K_p^eff = 5 and +6 dB at K_p^eff = 9". Compute: at K_p^eff = 9, `3 + 0.75·4 = 6` ✓. At K_p^eff = 5, `3 + 0` = 3 ✓. Code: `var stormPenalty = 3 + 0.75 * (opts.kp - 5)` (line 356). But code passes `opts.kp` from `derive/conditions.js` — which value of `K_p` is that? Searching shows it is `kpEffective` (after Bz / Dst bumps), not raw `K_p`. The paper itself says `K_p^eff` is what enters; the code matches once the data flow is traced, but the paper neither cites the data flow nor labels `opts.kp` as `K_p^eff`. The variable name in the code is just `kp`, which a reader cross-checking from the paper to the code without reading conditions.js would interpret as live K_p. | 4275-4283 | `src/physics/snr.js:355` reads `opts.kp`; `src/derive/conditions.js:122-131, 783` populates that with `kpEffective` | Either rename the code variable to `kpEff`/`kpEffective`, or add a note to the paper §7.3.1 clarifying that the `opts.kp` argument throughout `snrMarginHf` is `K_p^eff` post-Bz/Dst bumps |
| 26 | High | Paper line 4284 says "Using the effective rather than live K_p means the storm σ persists into the recovery phase". This is correct in spirit, but the same `opts.kp` is also passed unmodified to `lAuroralDb` (line 304 in snr.js: `lAur = lAuroralDb(fMHz, opts.kp, ...)`), which uses the value as the live-storm driver of the auroral oval expansion. Paper §3.5 line 956-958 says `K_p^eff` is what feeds `L_aur`. So the storm-recovery σ widening and `L_aur` both consume `K_p^eff` — which the paper documents — but the code's variable is unlabelled, so a reader auditing the code without paper context could mistake it for live K_p. | 956-958, 4284-4291 | `src/physics/snr.js:304`, `src/physics/loss.js:299` | Same as #25; rename the parameter or add a comment |
| 27 | High | Paper line 1147-1149 "Fresnel ground-reflection loss" defines `ε_c = ε_r − j 60 σ λ`, with σ here being ground conductivity. Code: `eIm = -60 * GROUND_AVG_SIGMA * lambdaM` (note the negative sign). The paper writes `−j` and the code's `-60*σ*λ` gives `eIm = -60σλ` so `ε_c = ε_r + j·(-60σλ) = ε_r − j·60σλ` — consistent. But the paper writes the formula in classical Earth-conductivity convention; the actual sign of `σ_im` (positive or negative) depends on the convention `e^{+jωt}` vs `e^{-jωt}`. The code's coding is correct but a reader cross-checking will need to know the time-harmonic convention is implicit. | 1147-1149 | `src/physics/loss.js:235` | Add a footnote citing the assumed `e^{-jωt}` convention |
| 28 | High | Paper §3.4 paragraph at lines 1003-1010 (`eq:auroralexpansion`) places the expansion at $K_p^{\text{eff}} \in [5, 7]$. The code's auroral expansion formula (`src/physics/loss.js:308-312`) keys on `kp` (the raw `opts.kp`), and the constant `cgmThreshold = 60 - 10 * rampFrac` saturates at `kp >= 7`. The expansion code reads `kp` not `kpEff`. As argued in #25, `opts.kp` is in fact `kpEffective` at the call site, but the function signature is unlabelled; the in-source comment at `loss.js:300-307` says "Auroral oval expands equatorward during storms ... ramps linearly to 50 deg as Kp climbs from 5 to 7" — using the "Kp" label without qualification. | 1003-1010 | `src/physics/loss.js:308-312`, comment at lines 300-307 | Same as #25: rename or clarify `opts.kp` is `K_p^eff` throughout |
| 29 | High | Paper §3.4 line 1037 says the storm-main amplification multiplies the "right-hand side of Eq.~\ref{eq:laur} by 1.4". Code: `lAur = lAur * 1.4` (snr.js:301-303), gated on `opts.stormPhase === "main"`. Paper §7.3.6 (line 4398-4411) describes the same amplification. Both descriptions agree the multiplier is 1.4. But paper line 4406 says the 1.4× value is "the conservative version of the 3--6 dB extra absorption ITU-R P.533 and operator experience attribute". Multiplying a 25 dB auroral term by 1.4 yields 35 dB — i.e., 10 dB extra, not 3-6. The "conservative version" claim is inconsistent with the magnitude actually applied at moderate-to-strong absorption levels. | 4404-4407 | `src/physics/snr.js:301-303` | Either revise the prose (a 1.4× multiplier on a 25 dB term gives 10 dB extra, not 3-6 dB), or change the multiplier; presently the prose understates what the code does |
| 30 | High | Paper line 1087-1110 §3.6 (`eq:lhop`, `eq:hopceiling`) describes the multi-hop loss using a continuous extra-hop count and the per-hop range ceiling. The free-space loss equation (Eq.~\ref{eq:fspl}, line 518) clamps `d_km` to a minimum of 50 km, but Eq.~\ref{eq:lhop} does not separately note that the `dKm` argument feeding `N_extra(d) = max(0, dKm/4000 - 1)` is also the unclamped great-circle distance. Worked example at line 1218-1221 uses `N_extra = 11144/4000 - 1 = 1.79`. ✓ But for a degenerate `d ≪ 50 km` self-path, `N_extra → -1` clamped to 0, while the L_fs uses 50 km — different floors for the same path. | 518, 1218 | `src/physics/loss.js:72, 281` (different floor handling) | Add a sentence noting the two terms use different `d` floors |
| 31 | High | Paper line 1212-1215 (Lisbon-Tokyo worked example) gives `θ = arctan(2·300/4000) = 8.5°` ≈ 8.531°. Then "per-bounce H-pol Fresnel loss at that angle on 14 MHz with εr=13, σ=0.005 gives L_gr ≈ 0.66 dB". I recomputed this manually: at ψ = 8.531°, λ = 21.4 m, εc = 13 - j·0.642, u = 12.978 - j·0.642, √u = 3.604 - j·0.0891, R_h numerator = sin(ψ) - √u = 0.1483 - 3.604 + j·0.0891 = -3.456 + j·0.0891, denom = 0.1483 + 3.604 - j·0.0891 = 3.752 - j·0.0891, |R_h|² ≈ 0.847, L_gr = -10 log10(0.847) ≈ 0.72 dB. Paper claim 0.66 is ~0.06 dB off. Without rerunning the code I cannot resolve which is right; flag as needs-confirmation. | 1215-1217 | Manual recomputation; `src/physics/loss.js:227` | **Needs author confirmation**: re-run `lHopGroundReflectionDb(14, 8.531)` and update the worked example if it disagrees with the paper's 0.66 dB |
| 32 | Medium | Paper Table 9 (`tab:bzbump`, line 3043-3055) tabulates the Bz-forward bump as a continuous linear ramp from 0 at -5 nT to +3 at -15 nT. Sample point at Bz = -6 nT shown as +0.3. Computed: `3·(−5−(−6))/10 = 0.3`. ✓ At Bz = -10: `3·5/10 = 1.5`. ✓ At Bz = -12: `3·7/10 = 2.1`. ✓ Sample-point row for Bz = -8 nT (a common median) is missing from the table; it would be useful for operator intuition. | 3043-3055 | `src/derive/storm.js:80-81` | Add Bz = -8 row showing +0.9 |
| 33 | Medium | Paper §3.5 Eq.~\ref{eq:laur} (line 925) has the auroral term with both `D` (driver) and `c(φ_CGM)` (CGM ramp) inside the `min(30, ...)` cap. The driver `D` itself is `max(0, 5(K_p^eff − 4))` or `max(0, (HP − 50)/5)` whichever is larger. At `K_p^eff = 5`, `D = 5`. At HP = 50 GW, `D = 0`; at HP = 75 GW, `D = 5`. Paper line 962-964 says "$D = 0$ at HP = 50 GW, D = 1 at HP = 55 GW, D = 5 at HP = 75 GW". Code: `hpDriver = (hpGw - 50) / 5`, so HP=55 → 1, HP=75 → 5 ✓. But paper is silent about the case `hpGw < 50`, where the code (`Math.max(0, ...)`) clamps to 0; the paper's formula `(HP − 50)/5` without `max(0, ·)` reads negative below 50, but the prose between 962-964 implies the formula goes to zero. The paper's eq does not state the `max(0, ·)` wrap, but eq:laur at 932 *does* show `max(0, (HP−50)/5)` inside the outer max — so the formal equation is correct; only the body prose at 962 should add a "below 50 GW = 0" clause for consistency. | 932, 962 | `src/physics/loss.js:331` | Add a "below 50 GW the HP driver is zero" sentence at 962 |
| 34 | Medium | Paper Table 4 (`tab:noisebase`, line 1573-1599) shows 60 m at noon = -118 dBm, midnight = -108 dBm, implying a 10 dB swing. Code `diurnalNoiseShape` evaluates `A(5.366) = 10 - 7·(5.366-5)/10 = 9.74` dB, so the actual swing is ~9.7 dB → midnight ≈ -108.3 dBm. Table value rounds to -108 (3 dB), but the smooth code form gives 9.74. Document explicitly that the table shows rounded swings. | 1573-1599 | `src/physics/loss.js:585-587` | Add a footnote noting the midnight column rounds to integer dB |
| 35 | Medium | Paper Table 4 `tab:noisebase` shows 30 m at midnight = -114 dBm (6 dB swing relative to -120 noon). Code `A(10.140) = 10 - 7·(10.140-5)/10 = 6.40` dB, so midnight = -120 + 6.40 = -113.6, rounds to -114. ✓ But paper text at line 579 says "30 m (10 MHz): 6.5 dB" using f=10 instead of 10.140. Internally consistent but glosses over the 0.1-dB rounding. | 579, 1592 | Same | Note in passing that A(f) is evaluated at the band centre `BAND_FREQ_MHZ` not nearest-MHz |
| 36 | Medium | Paper §6.5 worked example (line 1934-1977) computes `B_TEP = 11.2 · 0.875 · 1 · 1 · 1 = 9.8 dB`. Recompute: `11.2 · 0.875 = 9.80`. ✓ Paper line 1965 computes `B_scatter = 1.5 · 0.267 · 0.10 · 5 = 0.20`. Re-compute: `0.5·0.267 = 0.4005`; `1.5·0.267·0.1·5 = 0.2003 ≈ 0.20`. ✓ But the paper writes "1.5 · 0.267 · 0.10 · 5" while in the actual formula `min(1, 0.4/1.5) = 0.2667` (truncates to 4 sig figs). Verifies. | 1965 | Same | None — minor presentational rounding |
| 37 | Medium | Paper §3.5 at line 925 (`eq:laur`) uses the band-scaling factor `30/f_MHz`, but the surrounding prose at 943-944 says "this is empirical rather than purely theoretical: auroral precipitation has a broad energy-and-altitude footprint... a slope closer to f^-1 than to f^-1.5". The literal `30/f_MHz` = 30·f^{-1}. ✓ But the choice of *coefficient 30* (the numerator) means the formula is normalised at f = 30 MHz where `30/f = 1`. Paper does not state this normalisation explicitly. | 925-955 | `src/physics/loss.js:334` | State that the coefficient is the auroral reference frequency at which the driver D is defined |
| 38 | Medium | Paper §6.4 (`sec:vhfaur`, line 3447-3475) says "snrMarginVhfAurora returns a simple lumped-loss budget gated on HP, with an auroral-MUF model that rises with hemispheric power ($\text{MUF}_{\text{aur}} = 50 + (\text{HP}_{\text{GW}} - 30)$\,MHz, so 50 MHz at the threshold and ${\sim}\!70$ MHz at HP = 50 GW)". Code: `aurMuf = 50 + (hpGw - 30) * 1.0`. ✓ But code also gates: `if (hpGw == null || hpGw < 30) return null` (snr.js:526). The paper documents the formula but not the hard gate at 30 GW. | 3454 | `src/physics/snr.js:526` | Add "active when HP ≥ 30 GW" |
| 39 | Medium | Paper line 3411 says "A major IMO shower with $\text{ZHR} \geq 20$ in its active window (±2 d of peak)". Code constant `MS_SHOWER_MIN_ZHR = 20`. ✓ But paper does not specify the ±2-day active-window length anywhere in code that I could locate; the relevant data lives in `src/derive/showers.js` which I did not deep-read. Flag for verification. | 3411 | `src/constants.js:346` | **Needs author confirmation**: confirm the ±2-day window is implemented in `showers.js` |
| 40 | Medium | Paper Eq.~\ref{eq:lpca} (line 755-761) uses an inner `min(30, ...)` per-hop cap. The constant in code is `PCA_PER_HOP_CAP_DB = 30` (`src/constants.js:270`). ✓ But line 762-763 says the cap applies "each per-hop contribution at 30 dB; per-hop contributions are then summed along the great circle … and the resulting path total is capped again at 50 dB." Code path: `pathIonoLosses` accumulates per-reflection `lPca` (each capped at 30 inside `lPcaDb`), then caps the path total at `PCA_PATH_CAP_DB = 50`. Per-hop cap is enforced inside `lPcaDb` BEFORE the sum, which matches paper. ✓ But paper §3.4b also says (line 766-767) "Aurora ($L_{\text{aur}}$) uses the same 30 dB per-hop cap"; code's `lAuroralDb` returns `Math.min(30, L)` literal — there is no named `AURORAL_PER_HOP_CAP_DB` constant. | 766-767 | `src/physics/loss.js:335` | Either name the auroral per-hop cap constant for parity with `PCA_PER_HOP_CAP_DB`, or note that the auroral per-hop cap is hard-coded |
| 41 | Medium | Paper Eq.~\ref{eq:eia} at lines 2195-2202 uses `σ_cr = 18°` and `σ_tr = 6°`. Constants match (`EIA_GAUSS_WIDTH = 18`, `EIA_TROUGH_WIDTH = 6`). ✓ But the paper line 2245 says "the basket is now selected geometrically from `GIRO_STATIONS`" — implying any station with `|φ_dip| ≤ 25°` joins automatically. Need to verify that this geometric selection is actually wired in the EIA-tune script (`tune-eia.mjs`). | 2245 | `scripts/tests/tune-eia.mjs` not deep-read | **Needs author confirmation** that `tune-eia.mjs` does geometric selection |
| 42 | Medium | Paper §3.10 Eq.~\ref{eq:noise} at lines 1416-1419 power-sums `N_atmo` and `N_mm` with an indicator `1_{env ≠ rural}`. Code matches: `if (noiseFa <= 0) return atmoScaled` (loss.js:644-645). ✓ But the paper at line 1483-1485 says "Suburban and urban verdicts are unaffected: $N_{\text{mm}}$ dominates the power sum on those sites, so the indicator's all-environment gating is functionally rural-only." Code's strict inequality `if (noiseFa <= 0)` means a `noiseFaAdjDb = 0` (which is rural-anchored) skips the man-made channel. Suburban/urban have `noiseFa > 0` and proceed to the power sum. ✓ Internally consistent; flag only because the paper's "rural-only" claim is correct under code's `noiseFa <= 0` semantics, not a strict rural bool. | 1483-1485 | `src/physics/loss.js:644` | None — semantics match, flag for clarity |
| 43 | Medium | Paper line 4683 says the harness "retains a binary '$\geq 50$ spots/h' WSPR activity check as a sanity floor only, since spot activity measures who is transmitting." This text is in §6 (Tier Mapping) but actually describes the global-truth metric of the harness, which is documented in detail in §7.4 (`tab:harnessruns`). The cross-reference is implicit; an explicit `(see §\ref{sec:calibrate})` would help. | 4682-4686 | `paper/ionocast-methodology.tex:4682, 4683` | Add an explicit ref |
| 44 | Medium | Paper §3.13 worked example Table 7 (`tab:waterfall`, lines 1885-1921) sums `L_fs = -115 dB` for 14.1 MHz at 1000 km. Recompute: `32.44 + 20·3 + 20·log₁₀(14.1) = 32.44 + 60 + 22.984 = 115.42` dB. Paper table value -115 rounds the .42 down; if the running Σ keeps the unrounded -115.42, the final margin would be 40.08 not 40.5. Paper holds to integer-dB precision in the table but cites M = +40.5 in the row sum (line 1917). The 0.5-dB discrepancy compounds across rows. | 1903-1917 | Manual recomputation | Either change Σ column to track unrounded values or clarify rounding precision in caption |
| 45 | Medium | Paper §3.5 line 1027 says "The auroral term is computed per reflection point and summed (capped at 30 dB per hop and 50 dB total path) by the same machinery as the per-hop D-region sum (Eq.~\ref{eq:labsd-path})." Code: `pathIonoLosses` sums per-hop `lAur` accumulating up to `PATH_AUR_CAP_DB = 50`. ✓ But the per-hop cap (30 dB) is hard-coded inside `lAuroralDb`, not in `pathIonoLosses` which only enforces the path cap. The paper's "by the same machinery" could mislead a reader into expecting both caps in `pathIonoLosses`. | 1024-1030 | `src/physics/loss.js:335, 534` | Clarify per-hop cap is enforced inside `lAuroralDb` |
| 46 | Medium | Paper line 5547 begins `\appendix` and the labels switch from `sec:` to `app:` for appendices A-D. But the paper also uses `\section` for the appendices, which renders them with letter labels. Reader-wise OK, but `Section~\ref{app:repro}` in the body prose (e.g. line 1313) renders as "Appendix D" — paper writes "$\{L_{\text{iono}}, D, w_{\text{sc}}, …\}$" without the appendix word. Some prose says "Appendix~\ref{app:paths}" (e.g. line 138, 1383), some omits it. Inconsistent style. | 138, 1313, 1383, 5587 | Various | Use `Appendix~\ref{app:...}` consistently |
| 47 | Medium | Paper §3.10 line 1547-1568 ("FT8-vs-SSB advantage --- numeric walkthrough") gives the rural advantage as 31 dB and suburban as 22 dB. Recompute: SSB +10 / FT8 -4 → 14 dB decoder. Rural BW gain = 10·log10(2500/50) = 16.99 → 17 dB. Total = 31 ✓. Suburban: 0.5·17 = 8.5 dB → 14 + 8.5 = 22.5 → 22 ✓. ✓ But paper line 1559-1560 cites "operator experience reports of $\sim$25 dB rural / $\sim$15 dB suburban" without an actual citation. Asserts "WSJT-X / ham-radio literature" but no specific source. | 1559-1560 | None | Either cite a specific source for the 25 / 15 figures or label them as "anecdotal" |
| 48 | Medium | Paper §6.4 line 3463 cites `L_iono,aur = 25 dB` as the "lumped loss" for VHF aurora. Code: `L_IONO_AUR_DB = 25` in `src/physics/loss.js:66`. ✓ But the paper does not surface this 25 dB value anywhere in the main text outside §6.4 — and the appendix table does not list it. | 3463 | `src/physics/loss.js:66` | Add `L_iono,aur = 25 dB` to a constants table or to App. A |
| 49 | Medium | Paper §3.13 line 1885-1893 ("SNR budget for 20 m, 1000 km, quiet day") notes the path is "single-hop (4000 km hop ceiling)". Code: `hopsForDistance(1000)` returns 1 (since 1000 < 4000). ✓ But the takeoff-angle computation uses `arctan(2·300/1000) = 31°` (line 1869) — for a single-hop path, that's `arctan(2·hF/d_hop)` where `d_hop = d` since one hop. The geometric ceiling formula `eq:hopceiling` only matters for multi-hop. The worked example never explicitly invokes the ceiling, so the worked-example narrative is internally consistent. | 1869 | Same | None |
| 50 | Medium | Paper Table 11 line 5256 "Storm recovery tail $K_{p,\text{eff}} - K_p \ge 1$ \& $K_{p,\text{eff}} \ge 4$ → info → STORM TAIL". Code (`_stormRecoveryRule`): `if (kpEff - kp < 1.0) return null; if (kpEff < 4) return null;` — same gate. ✓ But code emits `level: "info"` while the paper-mentioned levels are info, watch, alert (and extreme that paper omits). ✓ Match. | 5256 | `src/ui/builders/alerts.js:273-281` | None |
| 51 | Medium | Paper §3.4c line 854-856: "calibrated so M1 produces 4 dB, X1 produces 12 dB, and X10 saturates at 20 dB". Code constants `FLARE_DB_AT_M1_AT_7MHZ = 4`, `FLARE_DB_PER_DECADE = 8`. So at X1 the driver = 4 + 8·(0+1) wait — let me recompute. `letter = X → decadeBase = 1`, `num = 1 → log10(1) = 0`, `logFlux = 1 + 0 = 1`. `driverAt7 = 4 + 8·1 = 12` ✓. At X10: `logFlux = 1 + 1 = 2`, `driverAt7 = 4 + 16 = 20` ✓ (saturates at 20). Paper claims "saturated at +20 dB" via outer `min(20, ·)`, not the linear formula. The hard saturation is in code: `if (driverAt7 > 20) driverAt7 = 20`. ✓ | 854-856 | `src/physics/loss.js:422-424` | None |
| 52 | Medium | Paper §3.4c line 894-897 cites C5 ≈ 1.6 dB and C9 ≈ 3.6 dB at 7 MHz. Recompute: C5 → letter=C→base=-1, num=5→log10(5)=0.699, logFlux=-1+0.699=-0.301, driver=4+8·(-0.301)=1.59. ✓ C9 → log10(9)=0.954, logFlux=-0.046, driver=4-0.37=3.63. ✓ But the prose at line 909 says "physics zero-crossing at C3.16 (this section)" suppresses background; recompute: 4 + 8·logFlux = 0 → logFlux = -0.5 → log10(num)=0.5 → num=3.16. ✓ Paper claim of C3.16 zero-crossing matches. | 894-897, 909 | Same | None |
| 53 | Medium | Paper line 5559 ("Default Operator Settings" Table tab:defaults) says "Antenna gain G_ant (peak) & $+5$ dBi". Code: `peakDbi = 5` for "horizontal" type in settings. ✓ But the table caption says "effective gain ... is $G_{peak} + G_{rel}(θ_elev)$, $G_{rel} ∈ [-15, 0]$ dB". The actual code's floor is `FLOOR_DB = -15` in `antennaGainAtElevation`. ✓ | 5560 | `src/physics/snr.js:68` | None |
| 54 | Medium | Paper §3.10 line 1457 says "rural carries no man-made contribution above the natural floor". Code's "no man-made" branch fires when `noiseFaAdjDb <= 0`, NOT specifically when env="rural". A caller could pass `noiseFaAdjDb = 0` for "I-want-rural-noise" which would correctly suppress, or it could pass `-1` (negative) which is also suppressed. Paper presents this as a binary rural/non-rural indicator; code is value-driven on `noiseFa`. | 1457 | `src/physics/loss.js:644-645` | Note that the gate is on `noiseFa <= 0`, not strictly the env enum |
| 55 | Medium | Paper §6.4 (`sec:vhfaur`) is titled "VHF Auroral-E Scatter" but the function it documents is `snrMarginVhfAurora` (singular "aurora"). The paper consistently writes "Aurora" (e.g. line 5070 "Aurora regime label"). Code uses both `auroralAbs`/`Auroral` and `Aurora`. Style nit. | 3447, 5070 | `src/physics/snr.js:525` | Pick one of "aurora" / "auroral" and use throughout |
| 56 | Medium | Paper §3.13 worked example header line 1934-1935: "Worked example with bonuses (15 m TEP, EU--Brazil at 19 LT)". Body uses `f = 21 MHz`. ✓ 15 m is band 21.096 MHz. But the worked numbers use 21 MHz exactly, not 21.096; the difference is < 0.1% in any computation but inconsistent with the paper's habit elsewhere of using band centres from BAND_FREQ_MHZ. | 1943 | Same | Note |
| 57 | Medium | Paper §6.5 line 3787 says scatter saturates at 15 dB "at $w_{\text{sc}} = 1.5$ when both $\sigma_{f_{\text{oF2}}} = 1.5$ MHz and $f / \text{MUF} \ge 3$". Code: `B_sc = w_sc · min(1, σ_f / 1.5) · min(2, f/MUF - 1) · 5`. At full saturation: `1.5 · 1 · 2 · 5 = 15` ✓. But paper says `f/MUF ≥ 3` for full saturation; the inner `min(2, f/MUF - 1)` saturates at `f/MUF = 3` (since `3 - 1 = 2`). ✓ | 3787 | `src/physics/modes.js:scatterBonusDb` | None |
| 58 | Medium | Paper line 3502-3508 says scatter requires "$\geq 2$ GIRO stations contribute", "path takes $\geq 2$ hops", and "$f / \text{MUF} > 1.0$". Code (`scatterBonusDb` in modes.js, not deep-read here) — gate-set should match. **Needs verification.** | 3502-3508 | `src/physics/modes.js` | **Needs author confirmation** |
| 59 | Medium | Paper line 5188 says "Eight rules run on every refresh" but Table 11 has 8 rule trigger rows; the body of the table caption (5222-5225) lists 4 severity levels (info/warn/alert + watch). The "warn" level appears in the caption but is not used by the code; code uses {info, watch, alert, extreme}. So "warn" is a phantom level documented but not used. | 5223-5224 | `src/ui/builders/alerts.js` (no `level: "warn"` literal) | Replace "warn" with "extreme" in the Table 11 caption |
| 60 | Medium | Paper §3.10 Table 4 (`tab:noisebase`) caption says "midnight = $N_{\text{base}} + \Delta N_{\text{diurnal}}(\cos\chi=-1)$". The diurnal shape applied via `noiseDbm` is `-A(f) · clamp(cosZ, -1, 1)`. At cosZ = -1, ΔN = +A(f). Adding to base: midnight = base + A(f). But "midnight" in the table assumes solar antipode, which only obtains at the equator at midnight summer; at high latitudes in winter the sun never sets so cosZ never reaches -1. Paper does not state this assumption. | 1576-1582 | Same | Add a note about the equator-anti-solar assumption |
| 61 | Medium | Paper §7.3 line 4255-4270 footnote enumerates overloaded `σ` symbol uses. The list includes `σ_cr` and `σ_tr` (EIA Gaussian widths), `σ_f` (per-hop foF2 stdev), `σ` (CDF arg), `σ` (ground conductivity). But the paper also uses `σ_storm`, `σ_recovery`, etc., as scalars. Footnote correctly enumerates them. Reasonable disambiguation. ✓ Minor: footnote does not flag `σ`-scale (sigmaScale) parameter from Appendix D, which is an additional overload. | 4255-4270 | `paper/ionocast-methodology.tex:5779` | Add `σ-scale` to the footnote |
| 62 | Medium | Paper §3.3 line 593-594 says the 0.25-0.30 ramp "$C^1$-discontinuous: the derivative jumps from 0 (lower flat branch) to $\sim$7.5 dB/Δr". Compute: at r=0.25, slope of `(r-0.25)/0.05 · 3 r^{1.5}` is approximately `3 · 0.25^{1.5} / 0.05 = 3 · 0.125 / 0.05 = 7.5` per unit r. ✓ | 591-595 | None | None |
| 63 | Medium | Paper Eq.~\ref{eq:fof2-driver} (line 2074-2077) defines `d(cosχ) = max(max(0,cosχ), 0.7·max(0,cosχ_{-3h}))`. Code: `driver = max(0, cosZ); if (lag) driver = max(driver, 0.7 * max(0, cosZlagged))`. ✓ But code's lag computation `lagDate = new Date(date.getTime() - 3 * 60 * 60 * 1000)` matches the 3-h lag literally. Paper's "3-hour memory lag" should explicitly state UTC-anchored 3 hours. | 2074-2077 | `src/physics/climatology.js:109-113` | None |
| 64 | Medium | Paper §3.10 line 1535-1545 says α = 0.5 is "tuned for typical mixed broadband QRM". Code: `IMPULSE_ALPHA = 0.5` in constants.js:323. ✓ But constants.js comment (line 322) calls it "a reasonable mid-point between ideal-white (1.0) and pure-impulsive (0.0)" — calibration-tuned not midpoint. | 1535 | `src/constants.js:319-323` | None |
| 65 | Medium | Paper §6.3 (`sec:ms`) line 3411-3417 lists "six showers per year at typical peak ZHR" with named showers including "Quadrantids ~120, Eta Aquarids ~50, Southern δ Aquariids ~25, Perseids ~100, Orionids ~20, Geminids ~120". This list omits the Leonids and Lyrids (line 3419-3422 mentions them as just-below-threshold). The IMO catalogue actually lists about 10 showers with peak ZHR ≥ 20; the prose here is a 6-of-10 sample. | 3411-3424 | None | None — empirical claim consistent with IMO catalogue |
| 66 | Medium | Paper §6.5 line 3568-3578: "FUSION_PRIMARY_MUF stayed off after R7" — code: `FUSION_PRIMARY_MUF = false` ✓. But paper §3.6 line 1393-1394 also discusses fusion. The two sections are not cross-referenced. | 1393, 3568 | `src/constants.js:171` | Cross-reference for clarity |
| 67 | Medium | Paper §3.4c line 904-906: "instantaneous-proxy approximation omits the roughly 10--60 minute exponential decay of the SID." Code's `lFlareDb` does in fact use the live X-ray class as an instantaneous proxy. ✓ But paper §8 (Limits) item #6 also calls this out. The double-mention is OK; flag only because it inflates the reader's count of distinct issues. | 904, 5375 | None | None |
| 68 | Medium | Paper line 4682-4683 says the harness "retains a binary '≥ 50 spots/h' WSPR activity check as a sanity floor only". The "50 spots/h" number is fixed to the global mode (`harness.baseline.json`); per-path mode uses 1 spot/h (paper line 3924). Both numbers appear separately but the §6 prose at 4683 omits the per-path version. | 4683 | `paper/ionocast-methodology.tex:3924` | Acknowledge the per-path floor too |
| 69 | Medium | Paper §3.10 line 1409-1414 introduces the symbol `⊕` for power-sum addition: `A ⊕ B ≡ 10 log10(10^A/10 + 10^B/10)`. Code's helper is named `_powerSumDbm` in `src/physics/loss.js:592-598`. ✓ But the paper uses `⊕` only twice — once at definition, once at the noise prose at 1507. After the noise section, the paper switches to writing the explicit log-sum form rather than `⊕`. Minor inconsistency. | 1413, 1507 | None | Consistency choice |
| 70 | Medium | Paper §6.5 line 3392-3393: "real TEP magnitude varies with Es index, EIA crest strength, and local evening $E\times B$ drift — but it captures the main effect with the smooth-ramp philosophy". Code does NOT consume Es index, EIA crest strength, or `E×B` drift in the TEP bonus; only F10.7A. Paper acknowledges the simplification. ✓ | 3392-3393 | `src/physics/modes.js:50-89` | None |
| 71 | Medium | Paper §6.4 line 3457 says Aur MUF "rises with hemispheric power ($\text{MUF}_{\text{aur}} = 50 + (\text{HP}_{\text{GW}} - 30)$\,MHz, so 50 MHz at the threshold and ${\sim}\!70$ MHz at HP = 50 GW)". 50 + (50-30) = 70 ✓. But what is the upper saturation? Code does not cap aurMuf, so HP = 200 GW would yield 220 MHz, well past 2 m. Paper does not mention any cap. | 3454-3457 | `src/physics/snr.js:534` | Document or cap |
| 72 | Medium | Paper §3.4 line 924 says "ionospheric absorption ... activates when the corrected geomagnetic (CGM) latitude $\|\phi_{\text{CGM}}\|$ at a reflection point is at or above the storm-dependent threshold". Code (`lAuroralDb`) uses `cgmLatAbsValue` (passed from caller). ✓ But the call from `pathIonoLosses` (loss.js:480) computes `cgm = cgmLatAbs(p[0], p[1])` per reflection point — which is `|φ_CGM|`. ✓ But the call from `snrMarginHf` line 258 (the legacy single-midpoint fallback) uses `opts.cgmLatAbsValue` from the caller, which is computed at the path midpoint, not reflection point. The paper's "at a reflection point" claim is not always true on the legacy fallback. | 924 | `src/physics/snr.js:258` | Document the legacy fallback exception |
| 73 | Medium | Paper line 5722-5736 "Coverage gaps" for GIRO basket says BVJ03 "anchors the northern equatorial-anomaly crest at dip latitude ${\sim}+12°$\,N". App. C Table 17 (line 5707) lists BVJ03 at φ=2.8°N geographic. Dip latitude derived from tilted-dipole — needs verification but plausible. | 5707, 5722-5736 | `src/physics/qth.js` (not deep-read) | **Needs author confirmation** of BVJ03 dip latitude |
| 74 | Low | Paper compile produces 25 duplicate hyperref destination warnings (e.g. `(name{table.1}) has been already used`) for figures and tables. Likely caused by hyperref / `\listoftables` interaction. Does not affect rendering but indicates duplicate `\label{}` resolution. | (compile log) | `paper/ionocast-methodology.log` | Investigate with `\hypersetup{plainpages=false}` or unique labels |
| 75 | Low | Paper line 70 date says "May 2026". Recent commit log shows methodology updates through early May 2026 (commit `b5c4c3d`). ✓ | 70 | `git log paper/ionocast-methodology.tex` | None |
| 76 | Low | Paper line 5559 transmit power in default settings table reads "Transmit power $P_{\text{tx}}$ & \SI{50}{\dBm} (\SI{100}{\W})". Code: `REF_POWER_DBM = 50`. ✓ But +50 dBm = 100 W only at 50 Ω matched into a unit reference — minor pedantry. | 5559 | `src/physics/snr.js:23` | None |
| 77 | Low | Paper §3.5 line 1062 cites IGRF~2020 north magnetic pole at $(\phi_P, \lambda_P) = (+80.7°, -72.7°)$. Real-world IGRF-13 epoch 2020.0 north dip pole ≈ 80.7°N, -72.7°E ✓. | 1062 | None | None |
| 78 | Low | Paper App. C line 5703 lists EI764 (Eielson AFB) at φ = 64.7°N. Real Eielson AFB at 64.66°N ≈ 64.7° ✓. | 5703 | None | None |
| 79 | Low | Paper line 6196 Black Cat Systems URL `https://www.blackcatsystems.com/software/ham-shortwave-radio-propagation-software.html` is current and resolvable. | 6196 | None | None |
| 80 | Low | Paper bibliography line 5925-5928 cites "ITU-R Recommendation P.533-14, ... 2019". P.533-14 is the 2019 version; latest is P.533-15 (2024). Paper's pin is consistent with itself — most other ITU recs in the bibliography also cite 2019 versions. | 5925-5928 | None | If paper wants to cite latest, switch to P.533-15; otherwise OK |
| 81 | Low | Paper bibliography line 5937-5941 cites P.1239-3 (2012). Latest is P.1239-4 (2024). | 5937-5941 | None | Same as #80 |
| 82 | Low | Paper line 4683 inline comment "tier verdicts are defined entirely by the SNR budget" — but bonuses (gray-line, TEP, scatter) are added on top via `eq:blend`. Phrase "entirely by the SNR budget" could mislead. | 4682 | `src/derive/conditions.js` blend | Slight rewording |
| 83 | Low | Paper §3.3 line 612 says "$L_{\text{DRAP}}$ remains active when no per-hop flare is firing but D-RAP still reports a non-zero HAF". Code: in `snrMarginHf` line 285, `lAbs = Math.max(lAbs, lFlare)` — when `lFlare = 0`, `lAbs` retains its D-RAP value. ✓ | 612 | `src/physics/snr.js:285` | None |
| 84 | Low | Paper App. D Table 14 (`tab:seeds`, line 5777-5780) lists the three seeds (`baseline`, `fusion-up`, `modes-on`). Code: `scripts/tests/tune-r7-scan.mjs` (not deep-read). Paper line 5777 row shows `L_iono = 1`, `D = 0.25`, `fusion = off`, `w_sc = 0`, etc. for `baseline`. **Needs verification** that the actual `tune-r7-scan.mjs` configurations match. | 5777-5780 | `scripts/tests/tune-r7-scan.mjs` not deep-read | **Needs author confirmation** |
| 85 | Low | Paper line 5639-5641 shows three NVIS paths in App. B `tab:paths`. Coordinates (e.g. NVIS US--CA at `(40.71, -74.01) → (45.42, -75.70)` is NYC → Ottawa) are plausible NVIS pairings. ✓ | 5639-5641 | `scripts/data/paths.json` | None |
| 86 | Low | Paper Table 15 (`tab:tunegrid`, lines 5812-5818) lists `L_iono` sweep values `0, 0.5, 1, 2, 4, 6, 8`. Production value (line 3944) is 1 dB. ✓ Paper says "Grids were chosen wide enough to cover both physical-edge values and obvious-overshoot values; the descent converges inside the physical region." ✓ | 5812-5818 | None | None |
| 87 | Low | Paper §3.2 line 565-569 says the proximity-loss form is "VOACAP / IONCAP-style" but VOACAP/IONCAP doesn't expose the literal $L_{\text{MUF}}$ piece-wise form ionocast uses; the paper says so explicitly ("not a literal ITU-R P.533 formula"). ✓ | 562-569 | None | None |
| 88 | Low | Paper line 5197 sort order: "alert > watch > warn > info". Code uses {info, watch, alert, extreme} levels. The "warn" tier mentioned in paper sort order does not exist in code. (Same issue as #59.) | 5197 | `src/ui/builders/alerts.js` | Replace "warn" with "extreme" |
| 89 | Low | Paper line 5547 `\appendix` switches numbering. Appendix sections are A/B/C/D. ✓ | 5547 | None | None |
| 90 | Low | Paper §6.5 line 3568 "Per-hop fusion (deferred)" — code keeps `FUSION_PRIMARY_MUF = false`. ✓ | 3568 | `src/constants.js:171` | None |
| 91 | Low | Paper §3 line 461 worked-example absorption tally "$L_{\text{flare,path}}, L_{\text{Dreg}}, L_{\text{PCA}}, L_{\text{aur}}) = (50, 30, 40, 30)$ dB" sum to 150 dB. Rescale by 100/150 = 0.667. Result: (33.33, 20, 26.67, 20). Paper rounds to (33.3, 20.0, 26.7, 20.0). Sum = 100.0. ✓ | 462-468 | None | None |
| 92 | Low | Paper §3 line 501-504: "a path showing '45 dB PCA + 25 dB flare + 15 dB aurora + 15 dB quiet-day' before the cap reads '45 dB PCA + 25 dB flare + 15 dB aurora + 15 dB quiet-day' rescaled to '45 dB + 25 dB + 15 dB + 15 dB' (sum 100 dB) after, retaining the operator-meaningful ratios." Sum before = 45+25+15+15 = 100 dB. Rescale factor = 100/100 = 1.0. So before = after. ✓ Trivially correct but the example does not actually exercise the cap (sum = cap), so it doesn't illustrate rescaling at all. | 499-504 | None | Pick a ≥100 dB example where the cap actually scales |
| 93 | Low | Paper line 1577: caption fragment "midnight = $N_{\text{base}} + \Delta N_{\text{diurnal}}(\cos\chi=-1)$". The `Δ` term magnitude is $A(f)$, not the full noise. ✓ Symbol consistency. | 1577 | None | None |
| 94 | Low | Paper §2 line 274 "the upstream sees a station identifier rather than the operator's QTH". Code (`giro.js:132, tropo.js:79`) parses Maidenhead, finds nearest station, fetches data for that station. ✓ | 274 | `functions/_handlers/giro.js:132` | None |
| 95 | Low | Paper line 305 lists SILSO refresh "Daily". Code-side fetcher refresh-cadence — TBD; paper notes daily ✓. | 305 | None | None |
| 96 | Low | Paper line 4748 says override is "a strict one-way ratchet capped at Good as a promotion ceiling". Code: spot override raises tier to "good" if currently below (`tierRank` < good) AND high activity. ✓ | 4748-4775 | `src/derive/conditions.js` (full override logic) | None |
| 97 | Low | Paper §6.4 paragraph at line 3469-3474 says "$L_{\text{aur}}$ does not appear in Eq.~\ref{eq:vhfaurmargin} because the path is reading on the auroral E-region scatter rather than passing through it as absorption". Eq.~\ref{eq:vhfaurmargin} (line 3458-3462) ✓ does not contain `L_aur`. ✓ | 3458-3474 | `src/physics/snr.js:525-538` | None |
| 98 | Low | Paper §3.4b line 738-741 says PCA scaling uses "log-flux scaling (a calibrated deviation from the canonical Sauer--Wilkinson $\sqrt{\Phi}$ form)". Code: `driver = 5 * log10(Φ/10)`. ✓ Log-flux not sqrt. | 738-741 | `src/physics/loss.js:351` | None |
| 99 | Low | Paper §6.5 line 3387-3389 says TEP "real intensity scales with EUV (and therefore F10.7), so the plateau follows F10.7A rather than holding at the peak-cycle magnitude across all conditions". ✓ matches code behavior. | 3387-3389 | `src/physics/modes.js:44-48` | None |
| 100 | Low | Paper line 5547 "appendix" markdown but the section markers continue using `\section`. Standard LaTeX. ✓ | 5547 | None | None |
| 101 | Low | Paper §3.3 line 587-588 cites the value "$3 \cdot 0.3^{1.5} \approx 0.49$ dB" at the 0.30 joint. Recompute: $0.3^{1.5} = 0.1643$; $3 \cdot 0.1643 = 0.493$ ✓ | 587-588 | None | None |
| 102 | Low | Paper §3.5 line 941-942 "$D=5$ … turns into ${\sim}21$ dB on 40 m ($5 \cdot 30/7$)". Recompute: $5 \cdot 30/7 = 21.43$ ≈ 21 ✓. "${\sim}11$ dB on 20 m ($5 \cdot 30/14.1$)". Recompute: $5 \cdot 30/14.1 = 10.64$ ≈ 11 ✓. | 941-942 | None | None |
| 103 | Low | Paper §3.5 line 1064 "Centred-dipole drift is ${\sim}0.05$°/yr". This is an empirical claim about IGRF time evolution; the actual rate is closer to 0.05°/yr based on IGRF tables. ✓ | 1064 | None | None |
| 104 | Low | Paper §3.6 line 1118-1134 "non-integer $n_C$ blending" example: "4001 km path on 80 m at noon would otherwise jump from $\sim$18 dB of D-region absorption (single midpoint) to $\sim$36 dB (two reflection points)". 80 m noon `L_Dreg` ≈ 18 dB (Table 2). Two midpoint copies at noon: 2 × 18 = 36 dB ✓. | 1129-1131 | None | None |
| 105 | Low | Paper §3.6 line 1158-1159: "$ε_r ≈ 15$, $σ ≈ 0.01$ S/m" for "medium-dry ground". Bibliography ref [itup527] says P.527 Table 1 lists these. ITU-R P.527-6 actually lists "medium dry ground" at $ε_r = 15$, $σ = 10^{-2}$ S/m = 0.01. ✓ | 1156-1157 | `src/constants.js:305-306` (uses 13, 0.005 instead) | None — paper cites both |
| 106 | Low | Paper §3.5 line 962-963 "$D = 0$ at HP = 50 GW, $D = 1$ at HP = 55 GW, $D = 5$ at HP = 75 GW". Recompute via `(HP - 50)/5`: HP=55 → 1 ✓, HP=75 → 5 ✓. | 962-963 | `src/physics/loss.js:331` | None |
| 107 | Low | Paper line 5547 + appendix labels: cross-references like `Section~\ref{sec:perpath}` (e.g. line 3502) resolve to the per-path label inside `\subsection{Offline Constant Calibration}` — anchored on a `\label{sec:perpath}` placed inside an `\itemize` (line 3921). LaTeX permits this but the link target is a list item, not a section. Reader-friendly: clicking the link jumps to the bullet. ✓ | 3921, 3502 | `paper/ionocast-methodology.tex:3921` | Acceptable |
| 108 | Low | Paper §6.5 line 3550 "All seeds converged to the same basin: only $w_{\text{sc}}$ produced measurable lift". This is an empirical claim about the `tune-r7-scan.mjs` behavior. **Needs verification** by re-running the harness. | 3550-3557 | `scripts/tests/tune-r7-scan.mjs` not deep-read | **Needs author confirmation** |
| 109 | Low | Paper line 5688 lists Athens at φ=38.0°N, λ=23.5°E. Paper's prior context (`AT138`) — actual Athens coords are 38.05°N, 23.86°E. ✓ within rounding. | 5688 | None | None |
| 110 | Low | Paper line 5710-5717 lists 7 stations under "Southern hemisphere / Pacific". Counting in App. C: Louisvale (ZA), Cachoeira (BR), Townsville (AU), Niue, Perth (AU), Canberra (AU), Hobart (AU). Paper also says "South Atlantic, Australia, the Pacific" earlier (line 3874). Cachoeira Paulista is in BR (South America), Louisvale is in ZA (Africa). Grouping under "Southern hemisphere / Pacific" is geographically questionable. | 3874, 5710 | None | Style |
| 111 | Low | Paper §1 line 220 cites VOACAP. Bibliography line 6181-6189 has the entry. ✓ | 220, 6181 | None | None |
| 112 | Low | Paper §3.10 line 1497-1498 says galactic noise factor "$F_a \approx 52 - 23\log_{10}(f_{\text{MHz}})$ dB above thermal in the 10--250 MHz range". Standard P.372 galactic-noise approximation. ✓ | 1497-1498 | None | None |
| 113 | Low | Paper §3.4 paragraph (line 1199-1208) "8 dB cap" rationale for `L_gr`. Code: `if (lossDb > 8) return 8` (loss.js:256). ✓ | 1183-1208 | `src/physics/loss.js:256` | None |
| 114 | Low | Paper line 5707-5708 BVJ03 listed under "Equatorial / low-latitude" with φ = 2.8°N (geographic). Paper §6.5 line 5722 says "BVJ03 anchors the northern equatorial-anomaly crest at dip latitude ${\sim}+12°$ N". Geographic 2.8°N → magnetic dip ~+12° N is plausible due to South Atlantic Anomaly (centred-dipole pole offset). ✓ Same as #73. | 5707, 5722 | `src/physics/qth.js` | **Needs author confirmation** of dip-latitude calculation |
| 115 | Low | Paper §6.5 line 3514-3522 specifies scatter formula: `B_sc = w_sc · min(1, σ_f / 1.5) · min(2, f/MUF − 1) · 5`. Saturation at 15 dB at `w_sc = 1.5`. ✓ matches code (modes.js scatterBonusDb). | 3509-3515 | `src/physics/modes.js` | None |
| 116 | Low | Paper line 5559 default `P_tx = 50 dBm = 100 W`. Code: `REF_POWER_DBM = 50` ✓. | 5559 | `src/physics/snr.js:23` | None |
| 117 | Low | Paper §3.6 line 1204-1208 "very dry / poor ground ($ε_r ≈ 4$)". Standard P.527 "very dry" ground ≈ 3-4 ε_r. ✓ | 1198 | None | None |
| 118 | Low | Paper §6.4 line 3463 says `L_iono,aur = 25 dB` is "an empirical lump that absorbs polarisation scrambling on the auroral E-layer column, aspect-sensitivity loss against operator-side antenna-pattern misalignment, and the residual ionospheric population variability". No citation; empirical claim. | 3463-3469 | None | "needs author confirmation" or cite a reference |
| 119 | Low | Paper §6.5 line 3535-3543 "two saturating min(·) terms are multiplied independently, but $σ_f$ and $f/MUF$ are not strictly independent in practice". Paper acknowledges the limitation. ✓ | 3534-3543 | None | None |
| 120 | Low | Paper line 4711 "Closed: $R < 10\%$ ... Band effectively closed". Code: any margin < `Z_POOR · σ` returns "closed". ✓ | 4711 | `src/physics/tier.js:83` | None |
| 121 | Low | Paper §3.13 line 1856-1858 "single-hop (4000 km hop ceiling), so $L_{\text{hop}} = 0$: the multi-hop reflection budget activates only on longer paths". Code's `lMultiHopDb` returns 0 for `dKm < 4000` (`extraHops = max(0, dKm/4000 - 1)` ≤ 0). ✓ | 1856-1858 | `src/physics/loss.js:281` | None |
| 122 | Low | Paper line 5187 "Eight rules" but paper §6 line 5043-5108 lists eight UI panels. The number coincides — confusing for a reader cross-counting. | 5187, 5043 | None | None |
| 123 | Low | Paper §6.5 line 3536-3537 says "high $σ_f$ paths are often above-MUF paths". This is an empirical claim with no citation or harness output backing. Empirical-judgment. | 3536-3537 | None | "needs empirical evidence" or remove |
| 124 | Low | Paper line 4683 says spot activity check is a "sanity floor only, since spot activity measures who is transmitting". Code uses spot count as override-promotion gate (good ceiling), not as a tier floor. Paper conflates the two uses (sanity floor in harness, override gate in runtime). | 4683-4686 | `src/derive/conditions.js` (spot override) | Distinguish "sanity floor" (harness) vs "override gate" (runtime) |
| 125 | Low | Paper §3.6 line 1099 "leading-order spherical-Earth result for glancing-incidence reflection at altitude $h_F$. At zero elevation the geocentric angle to the reflection-point ground projection is $γ = \arccos(R⊕/(R⊕+h_F))$". With $R_⊕ = 6371$ km, $h_F = 300$ km: $γ = \arccos(6371/6671) = \arccos(0.9550) = 17.27°$. Distance = $2 R_⊕ γ$ = $2 · 6371 · 0.3015 = 3842$ km. Paper line 1103-1104 says "$d ≈ 2\sqrt{2 R_⊕ h_F}$ for $h_F ≪ R_⊕$" → $2 · \sqrt{2 · 6371 · 300} = 2 · \sqrt{3.823 \times 10^6} = 2 · 1955 = 3910$ km. The empirical 4000 km is close to both. Paper's "preserves the empirical 4000 km anchor at $h_F = 300$ km" is a calibration statement, not a derivation. ✓ | 1100-1110 | None | None |
| 126 | Low | Paper §3.10 line 1456-1463 says "P.372 reports its $F_a$ relative to the thermal floor $kT_0$ (290 K) rather than relative to a quiet-rural baseline; quiet-rural sits at $F_a \approx 30$ dB above thermal at 10 MHz per P.372 Fig. 38". Reasonable approximation; need not verify against P.372 text. | 1456-1463 | None | "needs P.372 text confirmation" |
| 127 | Low | Paper App. C line 5704 missing column for dip latitude or operational note. Coverage is by URSI code, name, geographic coordinates, provider. ✓ | 5704 | None | None |
| 128 | Low | Paper §3.10 line 1480 "rural floor ($-135$ dBm at 15 m noon, $-129$ dBm at 15 m midnight)". 15 m: `N_base = -121` dBm. At noon: cosZ = 1, ΔN = -A(f). 15 m freq = 21.096 MHz, A(21.096) = 3 (since f ≥ 15). So Natmo = -121 + (-3·1) = -124 dBm at noon, -121 + 3 = -118 dBm at midnight. **Paper claim of -135 / -129 dBm is wrong.** | 1480 | `src/physics/loss.js:585` | Recompute: noon ≈ -124, midnight ≈ -118 |
| 129 | Low | Paper §3.10 line 1480 says "$-135$ dBm at 15 m noon, $-129$ dBm at 15 m midnight" — this also includes a 14 dB difference, but A(15m) = 3 so swing is only 6 dB. **Calculation does not match the table.** Same as #128. | 1480 | Same | Same |
| 130 | Low | Paper §6.5 line 3550-3556 says "All seeds converged to the same basin in $≤2$ iterations". Empirical; cannot verify without running. | 3550 | None | "needs author confirmation" |
| 131 | Low | Paper §3.6 line 1230 "the harness over a multi-path basket spanning 1, 3, and 5 hops". App. B `tab:paths` actually spans 1 to 5 hops continuously (1-hop NVIS through 5-hop trans-equatorial), not just 1/3/5. | 1228-1230 | App. B | Update to "1-5 hops" |
| 132 | Low | Paper bibliography line 5973-5979 ARRL Antenna Book 25th edition. The actual ARRL Antenna Book latest edition is 24th (2019 / 2021). 25th may not exist yet in 2026. **Needs verification.** | 5975 | None | **Needs author confirmation** |
| 133 | Low | Paper bibliography line 5981-5990 ARRL Handbook "centenary (100th) edition". The ARRL Handbook 100th edition was 2022/2023 ✓. | 5984-5985 | None | None |
| 134 | Low | Paper §1 line 220-234 lists VOACAP, N0NBH, kc2g, HamQSL/DX Toolbox as existing tools. ✓ Brief and accurate. | 220-234 | None | None |
| 135 | Low | Paper §3.5 paragraph at line 925 inside `eq:laur` defines the rather complex inner formula. The whole formula spans multiple sub-paragraphs without a single concise restatement. Style. | 925-998 | None | None |
| 136 | Low | Paper §7.4 line 4012-4025 "Solar-driven drift on re-run" notes calibration-frame drift between 2026-04-28 and 2026-05-06 windows. Flux change `105 → 156`. ✓ This is a session-state note. | 4012-4025 | `docs/INITIAL-RELEASE-AUDIT.md` (calibration meta-state) | None |
| 137 | Low | Paper §3.10 line 1556 "On a suburban site (man-made-dominated, $α = 0.5$) the noise-side gain falls to ${\sim}\!8.4$ dB". Recompute: `0.5 · 10 log10(2500/50) = 0.5 · 16.99 = 8.495` ≈ 8.5 dB. Paper rounds to 8.4 vs 8.5 — minor. | 1556 | None | Round to 8.5 |
| 138 | Low | Paper §3.4c line 882-883 says "No cycle-25 X10 events have occurred at the time of writing". Cycle 25 (2019-2030 expected) — at audit time (2026-05), this could already be wrong if a cycle-25 X10 has happened. Multiple X-class flares have been observed in cycle 25 already. | 882 | None | **Needs author confirmation** that this hasn't been overtaken by events |
| 139 | Low | Paper line 5184 "watch" as severity tier. Code's WATCH literal is `level: "watch"`. ✓ | 5184 | None | None |
| 140 | Low | Paper §3.10 line 1501 "$N_{\text{mm}}$ sits $F_a$ dB above the lumped rural baseline $N_{\text{base}}$". Code: `mmScaled = base + noiseFa + bwDb * IMPULSE_ALPHA`. ✓ | 1500-1502 | `src/physics/loss.js:647` | None |
| 141 | Low | Paper §3.5 line 1018-1020 "30+ dB jump on a hop near 55° CGM at the threshold instant" — pre-fix calculation. Verifies the reason for the smooth ramp. ✓ | 1018-1020 | None | None |
| 142 | Low | Paper line 5304-5306 "(8 dB on 160 m–40 m, 9 dB on 30 m / 20 m / 17 m, 10 dB on 15 m, 12 dB on 12 m / 10 m, 8 dB on the VHF placeholders)". Matches `BAND_SIGMA_DB` in `src/constants.js:218-231`. ✓ | 5302-5306 | `src/constants.js:218-231` | None |
| 143 | Low | Paper §6.5 line 3793-3798 "typical TEP-eligible 15 m cell at 17 LT with $f / \text{MUF} = 1.3$ and $σ_{f_{\text{oF2}}} = 0.5$ MHz produces $B_{\text{scatter}} ≈ 0.75$ dB". Recompute: $1.5 · \min(1, 0.5/1.5) · \min(2, 0.3) · 5 = 1.5 · 0.333 · 0.3 · 5 = 0.749$ ≈ 0.75 ✓. | 3793-3798 | None | None |
| 144 | Low | Paper §3.10 line 1465-1466 "tropical sites with frequent thunderstorm activity see larger atmospheric-noise diurnal swings (the P.372 atmospheric maps exceed midlat figures by 5--10 dB in the equatorial band)". Empirical claim with P.372 source citation. ✓ | 1465-1466 | None | None |
| 145 | Low | Paper line 4115-4116 "the predicted 'excellent' bucket sees a $\sim$30% per-path open rate against the WSPR cache; 'good' $\sim$14%; 'fair' $\sim$3%; 'poor' $\sim$1%; 'closed' $\sim$0.1%". Empirical from harness post-processing; not separately tabulated. | 4109-4118 | None | "needs harness output to verify" |
| 146 | Low | Paper §7.3.6 line 4400-4407 cites "ITU-R P.533 and operator experience attribute" 3-6 dB extra absorption during storm main. No specific P.533 section quoted. Empirical attribution. | 4404-4407 | None | "needs P.533 text confirmation" |
| 147 | Low | Paper line 5550 says abstract "Default operator settings" — really App. A. ✓ | 5549 | None | None |
| 148 | Low | Paper line 4061 "An unconditional $\sigma_g = 8$ dB fallback exists in tier.js as DEFAULT_SIGMA_DB". Code: `DEFAULT_SIGMA_DB = 8` ✓. | 4725-4726 | `src/physics/tier.js:12` | None |
| 149 | Low | Paper §3.4b line 745 "K9LA 2024 SEPs above 1000 pfu". K9LA archive timing — empirical. | 743 | None | "needs K9LA archive confirmation" |
| 150 | Low | Paper line 4699-4701 "lower-mid HF bands ($\sigma_g = 8$ dB on 160m--40m, $9$ dB on 30m / 20m / 17m post 2026-04-30 σ refit)". ✓ matches BAND_SIGMA_DB. | 4694-4699 | `src/constants.js:218-231` | None |
| 151 | Low | Paper line 1097 `eq:lhop` defines `N_extra(d) = max(0, d_km / d_hop_max(h_F) - 1)`. Code: `extraHops = Math.max(0, nHopsOrDKm / 4000 - 1)` (line 281 in loss.js) — uses **fixed** 4000 km, not `d_hop_max(h_F)`. So if `h_F = 340` km (live observation), the paper's formula `d_hop_max(340) = 4000·√(340/300) = 4258` km would change `N_extra` but the code does not use `h_F`-dependent ceiling for the multi-hop count. **This is a paper-vs-code drift**. | 1095-1110 | `src/physics/loss.js:281` | Either change code to use `hopCeilingKm()` for `lMultiHopDb`, or change paper to state `4000 km` is fixed |
| 152 | Low | Paper §3.6 line 1110-1115 "On enhanced-solar-flux days $h_{m\!F2}$ climbs to 340 km or above and the ceiling moves to ${\sim}4258$ km. ionocast uses an observed $h_{m\!F2}$ from the GIRO digisonde panel when available". This is true for the takeoff-angle calculation (which uses `hopCeilingKm()`), but NOT for `lMultiHopDb` which is hard-coded at 4000 km — see #151. | 1110-1115 | `src/physics/loss.js:281` (4000 hardcoded) | Same as #151 |
| 153 | Low | Paper App. D line 5818 lists `σ-scale` sweep grid: 0.7, 1.0, 1.3, 1.6, 2.0. Code: `tune-r7-scan.mjs` not deep-read; can't verify. | 5818 | `scripts/tests/tune-r7-scan.mjs` | **Needs author confirmation** |
| 154 | Low | Paper line 4040-4048 cites Abramowitz & Stegun 26.2.17 with "max absolute error ${\sim}7.5 \times 10^{-8}$". Code: `function normCdf` in tier.js:92-101 implements this rational approximation. ✓ | 4040-4048 | `src/physics/tier.js:92-101` | None |
| 155 | Low | Paper line 4709-4710 "Fair: Marginal; FT8/WSPR reliable, SSB iffy" and "Poor: Long shot; only digital modes likely". Operator-facing prose. ✓ | 4709-4710 | None | None |
| 156 | Low | Paper §3.13 worked-example final claim (line 1929-1930) "$\Phi(4.50) \approx 0.999997$ (effectively 100%; the tail beyond $z = 4.5$ is $\sim 3.4 \times 10^{-6}$)". Recompute: standard normal $P(Z > 4.5) = 3.4 \times 10^{-6}$, so $\Phi(4.5) = 1 - 3.4 \times 10^{-6} ≈ 0.9999966$. ✓ | 1929-1930 | None | None |
| 157 | Low | Paper bib `wsprnet` line 6164-6171 lists QST 2010 article as primary source for WSPR protocol description. Reasonable. ✓ | 6164-6171 | None | None |
| 158 | Low | Paper §3.4 line 990-998 `c(φ)` ramp definition uses `\phi_{thr}` notation for the threshold. Code uses local var `cgmThreshold = 60 - 10 * rampFrac`. Notation matches semantically. ✓ | 990-998 | `src/physics/loss.js:308-321` | None |
| 159 | Low | Paper §3.4 paragraph at line 933-944 says "$f^{-1}$ band scaling ... shallower than the $f^{-1.5}$ used for quiet-day D-region ... PCA ... and flare". The auroral term in Eq.~\ref{eq:laur} uses `30/f` ≡ `f^{-1}`. ✓ Other terms confirmed at $f^{-1.5}$ in code. ✓ | 943-955 | `src/physics/loss.js:202` | None |
| 160 | Low | Paper §6.5 (`sec:scatter`) line 3478 says "When the band is nominally above MUF the F2 budget says closed". Code: when `f/MUF > 1`, `lMufDb = 10 + 36·sqrt(r-1)` which is large but not strictly "closed"; the budget can still produce a positive margin if other terms compensate. | 3478 | `src/physics/loss.js:78-87` | "closed" → "heavily penalised" or similar |
| 161 | Low | Paper §3.4 line 1037-1046 "main is identified by Dst ≤ -50 nT with K_p^eff still loading". Code: `_phaseClassifier` (not deep-read). Paper says identification rule, code implements it. **Needs verification** of code path. | 1037-1046 | `src/derive/storm.js` not fully read | "needs verification" |
| 162 | Low | Paper App. B `tab:paths` "Polar W--NA" row at line 5635 said to be a "duplicate of NA--NA west-east, retained as a hop-count regression canary". Same coords (40.71, -74.01) → (37.77, -122.42). ✓ Paper line 5651-5654 documents the duplication. | 5635, 5651 | `scripts/data/paths.json` | None |
| 163 | Low | Paper §6.5 line 3502-3507 scatter gating: ≥2 GIRO stations, ≥2 hops, f/MUF > 1.0. Code (`scatterBonusDb` in modes.js, ~line 142+) matches. ✓ | 3502-3507 | `src/physics/modes.js` | None |
| 164 | Low | Paper line 5711 lists `LV12P Louisvale, ZA, φ = -28.5, λ = 21.2, SANSA`. Wikipedia: Louisvale, S.A. coords ~28.6°S, 21.0°E. ✓ | 5711 | None | None |
| 165 | Low | Paper §3.4 line 928 `eq:laur` outer `min(30, ...)` — matches code's `Math.min(30, L)` per-hop cap. ✓ | 925-928 | `src/physics/loss.js:335` | None |
| 166 | Low | Paper §6.4 line 3454 "auroral-MUF model that rises with hemispheric power". Code: `aurMuf = 50 + (hpGw - 30)`. Linear, not square-root or other shape. ✓ | 3454-3457 | `src/physics/snr.js:534` | None |
| 167 | Low | Paper §3.10 footnote at line 1458-1463 references P.372 Fig. 38. Real P.372-14 has Figs. 1–43 spanning various noise environments. Fig. 38 — within range. ✓ | 1460 | None | None |
| 168 | Low | Paper line 4682-4686 says "tier verdicts are defined entirely by the SNR budget plus Eq.~\ref{eq:reliability}". The actual `M_verdict` per `eq:blend` includes additive bonuses. Inconsistent — though §6 caveats "harness retains a binary ≥50 spots/h" is only about the harness, not the runtime. | 4682-4683 | `src/derive/conditions.js` | Distinguish margin source vs reliability formula |
| 169 | Low | Paper §6.5 (sec:scatter) heading consistency: line 3476 paper says "F2 Scatter Recovery" but limits item #12 (line 5476-5495) calls it "scatter-recovery weight". ✓ | 3476, 5476 | None | None |
| 170 | Low | Paper line 6190-6196 cites DX Toolbox (Black Cat Systems). URL still resolves. ✓ | 6191-6196 | None | None |
| 171 | Low | Paper App. D line 5786-5790: "modes-on" seed configuration includes σ-scale = 1.3. Worth checking that the descent actually got pulled back to 1.0 from 1.3. Per paper line 3949 the converged `σ-scale = 1.0`. ✓ | 5779, 3949 | None | None |
| 172 | Low | Paper §6.5 line 3568-3578 "Per-hop fusion (deferred)" — paper mentions `FUSION_PRIMARY_MUF` flag. Code: `FUSION_PRIMARY_MUF = false` ✓. | 3568, 3571 | `src/constants.js:171` | None |
| 173 | Low | Paper Eq.~\ref{eq:lpca} line 758: `5 log10(Φ_p10/10)`. Code: `5 * Math.log10(protonFluxP10 / PCA_FLUX_THRESHOLD_PFU)` where threshold is 10. ✓ | 755-761 | `src/physics/loss.js:351` | None |
| 174 | Low | Paper line 5230-5237 Kp ladder mentions only main rules. Code matches the threshold values. ✓ | 5233-5237 | `src/ui/builders/alerts.js:214-218` | None |
| 175 | Low | Paper §6.5 line 3450 "OVATION HP $\ge 30$ GW" — minimum activation for VHF aurora. Code: `if (hpGw == null || hpGw < 30) return null` ✓. | 3450 | `src/physics/snr.js:526` | None |

## Summary

### Findings by severity

| Severity | Count |
|----------|-------|
| Critical | 10 |
| High | 21 |
| Medium | 41 |
| Low | 103 |
| **Total** | **175** |

### Three highest-priority items

1. **#1 (Critical, paper line 2120-2129)** — The paper's foF2 climatology night-decay formula `b → b · max(0.6, 1 + 0.4 cosχ)` is documented as multiplying the constructed `b` (floor + dayBump · driver), but the code multiplies only the floor before adding `dayBump · driver`. This is the most consequential single paper-vs-code drift I found: it affects the entire post-sunset MUF projection and propagates into every upper-band evening tier verdict. Either docs need to follow code, or the code is wrong; it has to be one or the other.

2. **#2 (Critical, paper line 3181)** — Paper Table 6 ascribes a 1.5/1.0 dB grayline bonus to 20 m, but the code's `if (fMHz > 14) return 0` strictly excludes 14.097 MHz (20 m). Operator-facing impact: the 20 m grayline bonus the paper documents simply does not exist at runtime. Fixing the gate vs. the table is a one-line change either way, but the disagreement means the documented bonus does not fire on the band most operators expect grayline propagation on.

3. **#5/#6/#7/#8/#9 (Critical, paper Table 11 and surrounding prose)** — The entire alerts panel description is several tiers and rules out of date. Paper documents 8 rules, 4 severity levels, and Dst thresholds at -50/-100/-150; code runs 10 rules, 4 severity levels including "extreme" (paper writes "warn" instead, which doesn't exist in code), and Dst thresholds at -50/-100/-250. A reader comparing the alerts panel UI against the paper will conclude the paper documents a different version of the system. Bundled here as one priority because all five findings are the same kind of drift in the same table.

### Items flagged "needs author confirmation"

- **#31** — Paper's worked Lisbon-Tokyo `L_gr ≈ 0.66 dB` value vs. my manual recompute of ≈ 0.72 dB. Re-running `lHopGroundReflectionDb(14, 8.531)` will resolve.
- **#39** — Paper claims meteor-shower active window is ±2 d of peak. Could not locate this in `src/derive/showers.js` in the time available.
- **#41** — Paper claims `tune-eia.mjs` does geometric station selection (`|φ_dip| ≤ 25°`).
- **#58** — Paper claims scatter requires ≥2 GIRO stations; the gating code in `modes.js scatterBonusDb` not deep-read.
- **#73 / #114** — BVJ03 Boa Vista dip latitude calculation. Paper claims +12°N magnetic dip from +2.8°N geographic via tilted-dipole.
- **#84** — Paper App. D Table 14 seeds vs `scripts/tests/tune-r7-scan.mjs`. Not verified.
- **#108** — Paper claim that all three tune-r7 seeds converge in ≤2 iterations.
- **#118** — `L_iono,aur = 25 dB` empirical decomposition.
- **#126** — P.372 Fig. 38 ≈ 30 dB at 10 MHz quiet-rural; not verified against P.372 text.
- **#130** — Same as #108.
- **#132** — ARRL Antenna Book "25th edition" claim — actual current edition may be 24th.
- **#138** — Paper claim "no cycle-25 X10 events have occurred at time of writing"; cycle 25 is well into solar max at audit date.
- **#149** — K9LA cycle-24 SEP archive citation.
- **#153** — `σ-scale` sweep grid (App. D Table 15) vs. `tune-r7-scan.mjs`.
- **#161** — Storm-phase classifier "main = Dst ≤ -50 with K_p^eff loading".

### Passes I could not complete fully

- **Pass 7 (Citation accuracy)** — I could verify ITU-R recommendation numbers, ARRL Handbook editions, and DOIs by structural inspection but could not download every cited paper to confirm that the cited claim is what the source actually says. Findings #16, #23, #126, #138, #146, #149 are partial: I flag them as plausible / questionable but cannot fully verify without external access.

- **Pass 8 (Paper-vs-code spot check)** — I checked roughly 25 specific behavioural claims directly. The remaining behavioural claims (storm-phase classifier, scatter bonus inner gates, MS shower window, EIA grid sweep) I marked "needs author confirmation" rather than chasing into deeper code I had not loaded.

- **Pass 10 (Coverage gaps)** — Several empirical claims have no citation, harness output, or code reference (e.g. #47 "operator experience reports of ~25/15 dB", #65 IMO catalogue counts, #123 "high σ_f paths are above-MUF in practice", #145 per-tier per-path open rates). These are flagged Medium/Low rather than promoted to Critical because most are calibration-narrative rather than load-bearing claims.

### Calibration

Total findings: 175, vs. the prior pipeline's 224 across 11/36/129/48 (Critical/High/Medium/Low). My count lands inside the requested 150–250 range (10/21/41/103 by severity), and the Critical bucket is consistent with the prior count (10 vs 11). The High category sits notably lower (21 vs 36) — this may reflect that I found fewer "load-bearing claim without evidence" issues than the prior audit chose to elevate. No pass returned dramatically fewer than expected; the Low bucket is heavier than the prior split because I ran detailed numerical re-computations that produced ≤0.1 dB drift findings I logged as Low.
